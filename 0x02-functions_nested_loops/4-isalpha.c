#include "main.h"

/**
 * _isalpha - checks for alphabetic character
 *  * Return: 1 if c is a letter, 0 otherwise
 * XDD - XDD, sXDDh 0XD
 * @XD: XDDDDDDDDD
 */

int _isalpha(int c)
{
	return ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'));
}

